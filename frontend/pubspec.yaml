name: phtoo
description: "AI Photo Selector App"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.1.2
  file_picker: ^8.1.2
  http: ^1.2.1
  flutter_appauth: ^7.0.0
  google_sign_in: ^6.2.1
  http_parser: ^4.0.2

  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true